---
share: "true"
---

The [[undefinedhidden/Docker|Docker]] [[daemon|daemon]] is a persistent process that manages [[undefinedhidden/Docker Container|Docker Container]]. 
It listens for requests via the [[Docker Engine|Docker Engine]] APIs

This is run by `dockerd`

